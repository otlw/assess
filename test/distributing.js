var ConceptRegistry = artifacts.require("ConceptRegistry");
var Concept = artifacts.require("Concept");
var Distributor = artifacts.require("Distributor");

utils = require("../js/utils.js")
// var deploymentScript = require("../migrations/2_deploy_contracts.js")

contract("Distributor", function(accounts) {

    it("is knows about the mew-concept", async () => {
    })

    it("can create concepts and store their address", async () => {
    })

    it("can a add a user as member to mew (with a given weight)", async () => {
    })

    it("can not add more members to mew than specified", async () => {
    })
})
